# Cast v2 comparison gate

W3.4 was evaluated against the committed `modern-format-synthetic` corpus with:

```bash
uv run python apps/api/scripts/run_eval.py \
  --cast-v2 \
  --output-json /tmp/echodraft-cast-v2-eval.json \
  --output-md /tmp/echodraft-cast-v2-eval.md
```

| Metric | 2026-07-07 baseline | Cast v2 | Gate |
|---|---:|---:|---|
| Cast precision | 1.00 | 1.00 | pass |
| Cast recall | 1.00 | 1.00 | pass |
| Cast F1 | 1.00 | 1.00 | pass |
| Alias-cluster purity | 1.00 | 1.00 | pass |
| Merge error rate | 0.00 | 0.00 | pass |
| Split error rate | 0.00 | 0.00 | pass |
| Attribution accuracy | 1.00 | 1.00 | unchanged |
| Total flags | 3 | 2 | improved |

The fixture already has no duplicate cast rows, so its duplicate error rate cannot fall below
zero; cast v2 preserves that result and removes one warning. The local
`qwen3-embedding` model was not installed during this run, so the live harness exercised the
documented conservative string-only fallback. Deterministic tests cover embedding-weighted alias
merges, transitive agglomeration, same-window speaker cannot-link constraints, prior confirmed and
rejected rulings, one reconciliation call per pooled cluster, and additive profile/manifest output.
