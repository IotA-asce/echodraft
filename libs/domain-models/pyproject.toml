[project]
name = "echodraft-domain-models"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.11.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/echodraft_domain"]
